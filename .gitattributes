# 여러 워크트리(세션)가 병렬로 CHANGELOG.md/docs/DECISIONS.md 상단에 항목을 추가하는
# 패턴이 잦아, merge=union으로 충돌 없이 양쪽 내용을 모두 남긴다.
# 주의: union merge는 순서를 보장하지 않고 중복 라인을 걸러내지 않는다 —
# 머지 후 [Unreleased] 섹션 배치·중복 여부를 육안으로 확인할 것.
CHANGELOG.md merge=union
docs/DECISIONS.md merge=union
